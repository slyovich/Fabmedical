{
  "name": "fabmedical-unit-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Integration tests for Service Bus sqlFilter behavior",
  "scripts": {
    "emulator:up": "docker compose -f docker-compose.test.yml up -d",
    "emulator:down": "docker compose -f docker-compose.test.yml down -v",
    "test": "jest --runInBand",
    "test:local": "npm run test",
    "test:host": "npm run emulator:up && npm run test && npm run emulator:down",
    "test:ci": "npm run test"
  },
  "devDependencies": {
    "@azure/service-bus": "^7.9.5",
    "jest": "^29.7.0"
  }
}
